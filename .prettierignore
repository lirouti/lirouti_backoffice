styled-system
dist
node_modules
design
public/riruti-icons.svg
public/riruti-assets.svg
bun.lock

# 마크다운 — 표 정렬이 한글에서 역효과다
#
# prettier 는 표 칸을 공백으로 패딩해 맞추는데, **글자 수로 세고 화면 폭으로 세지 않는다.**
# 한글은 한 글자가 두 칸이라 정렬이 되지도 않으면서 줄만 길어진다 — `ARCHITECTURE.md`
# 하나에서 600줄이 그렇게 바뀌었다. 코드와 달리 얻는 것이 없다.
*.md
docs/
