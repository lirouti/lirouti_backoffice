styled-system
dist
node_modules
design
public/riruti-icons.svg
public/riruti-assets.svg
bun.lock

# 마크다운 — 표 정렬이 한글에서 역효과다
#
# prettier 는 표 칸을 공백으로 패딩해 맞추는데, **글자 수로 세고 화면 폭으로 세지 않는다.**
# 한글은 한 글자가 두 칸이라 정렬이 되지도 않으면서 줄만 길어진다 — `ARCHITECTURE.md`
# 하나에서 600줄이 그렇게 바뀌었다. 코드와 달리 얻는 것이 없다.
*.md
docs/

# 다른 도구가 읽는 설정 — 우리 코드 스타일을 강제할 이유가 없다
#
# prettier 는 여기서 큰따옴표를 작은따옴표로 바꾸기만 한다(YAML 에서는 같은 값이다).
# 얻는 것은 없고 잃는 것은 둘이다.
#  · `.coderabbit.yaml` 은 **백엔드 저장소와 같은 설정**을 쓰는 파일이라, 우리 쪽만
#    바꾸면 두 저장소를 대조할 때 지저분해진다. 게다가 이 파일을 건드리면 CodeRabbit 이
#    「설정이 바뀐 PR」 경고를 달고 **대상 브랜치의 설정으로** 리뷰한다.
#  · 이슈 템플릿은 GitHub 이 읽는 폼 정의라 그쪽 문서 표기를 따르는 편이 낫다.
.coderabbit.yaml
.github/

